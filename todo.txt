
# Claude's ideas 2nd Feb

  [Done] Lazy SMP (multi-threaded search) -- The engine is single-threaded. On modern hardware this leaves a lot of performance
   on the table. Lazy SMP is the standard approach: multiple threads share the transposition table and search the same
  root position, relying on hash collisions and timing differences for natural work distribution. This is probably the
  single biggest strength gain available.

  Texel tuning -- Every eval parameter (mobility arrays, king safety weights, pawn bonuses, passed pawn values, etc.) is
   hand-tuned. A Texel tuning framework uses a large set of labeled positions (from engine self-play or databases) to
  optimize all constants via gradient descent against game outcomes. This typically finds non-obvious parameter
  interactions that humans miss.

  (in progress - first version failed, tuning mad it netural) Continuation history -- Currently the engine has basic from-to history and counter-moves. Continuation history
  (indexing by the previous 1-2 moves' piece-to-square) significantly improves quiet move ordering, which makes LMR more
   effective and reduces the search tree.

  [Done] SEE pruning of quiet moves in main search -- The engine already uses SEE in quiescence, but applying it to prune
  losing captures and bad quiet moves in the main search (at low depths) is relatively cheap to implement and reduces
  the tree further.

  Syzygy tablebase support -- Perfect endgame play with 6-7 pieces. There are existing Go libraries for probing Syzygy
  tables, so the integration effort is moderate.

  Of the five, Lazy SMP and Texel tuning would give the most visible improvement. Lazy SMP is the more self-contained
  project; Texel tuning requires building a dataset of positions with game results first. What interests you?


# Next things to look at:

* Texel tuning (coded, training underway)
* Syzgy tablebase


# Bugs
This position
  r4r1k/p2bp1b1/qpp2pB1/2Pp4/Pn1P4/4PNP1/1P1N1KP1/RQ3R2 b - - 2 22
We seem to evaluate at +3 which seems way out, and later proves false.

The current Texel output performs much worse due to scaling. I think Claude has fixed this. But code is again all mixed in.


Texel Training Regression ends up sorting the file which is somehow taking 1.2GB or more of RAM. Scrap sort.

Claude did some weird optimisation of allocation, but it's not helped the RAM Usage. Current code
includes multiple changes. One is to make sure the piece trading stuuff is tunable (keep), the
optimsation is bad.

The RFP implementation looks correct but conservative. Many engines extend this to depth 5-6 with scaled margins. Currently margin = depth * 120, which is reasonable, but the depth cutoff limits its effect.

5. No history penalty for quiet moves that don't cause cutoffs

At line 629, history is incremented on beta cutoff, but quiet moves that were tried and didn't cause cutoffs aren't penalized. Adding History[from][to] -= depth * depth for all quiet moves tried before the cutoff move improves move ordering measurably — the table then captures relative quality, not just absolute cutoff counts.

6. TT is single-entry per bucket (tt.go:82-98)

Depth-preferred replacement means shallow-but-recent entries get thrown out by deep-but-stale ones. A two-entry bucket (one depth-preferred, one always-replace) is a common improvement that reduces collision losses with minimal complexity.

7. No SEE pruning for quiet moves in main search

SEE is used for capture ordering in the move picker and for quiescence pruning, but not for pruning quiet moves at shallow depths. Adding if depth <= 3 && SEESign(move, -threshold) { skip } for quiet moves that lose material can prune bad tactical moves early.

8. Missing: delta pruning in quiescence

In quiescence, every capture is searched if SEE >= 0, regardless of how far below alpha the stand-pat score is. A delta cutoff (if standPat + maxPossibleGain + margin < alpha { return alpha }) avo

# Adam's ideas

Eval hashes (done)
Eval seems pretty simple still.
* Do we consider rooks on open file, doubled rooks, rooks behind passed pawns.
NPS Performance still trails GNU Chess by 20%
Extensions?
* are we generating capture moves twice in move generator
* Do we have any end game knowledge (e.g avoid trading down into minor and king End game)
* end game table bases? We don't know how to play a KP vs K end game. Even strugglign with a KQ vs K mate
* Taper score if rep-counter or 50 move counter are high.
* Are we properly claiming a 50 move or rep draw in UCI mode?

* Opposite colour bishops in an EG is drawy.
* Swap material if up, pawns if behind. Encouraging trading when ahead.


Cosmetics:
* CLI mode to make it easier to test positions, and debug how we score positions. (done)
* Max depth report e.g. 12/17
* With EPD report the time we first got it right (and didn't later get it wrong). (done)
* Use the solve time to generate points in some way - gives a better indication across more positions than just right/wrong.
* Run a particular set of puzzles from an EPD.


1B6/8/8/4bp2/4k1p1/6P1/8/6K1 w - - 98 129  - our contempt may have stopped us accepting a drawn game
8/8/8/5p2/3k2p1/6P1/5K2/8 w - - 2 131  - we don't know we-ve lost


# EPD performance

## WAC Failures (at 100ms):
WAC.071, WAC.080, WAC.081, WAC.087, WAC.100, WAC.109, WAC.116, WAC.141,
WAC.151, WAC.163, WAC.180, WAC.196, WAC.213, WAC.222, WAC.237, WAC.265,
WAC.283, WAC.293

Primary pattern: 8/18 require quiet moves (no check/capture) that get LMR-reduced or LMP-pruned.
Secondary pattern: 5/18 involve mating attacks starting with quiet moves.
Minor: 2-3 endgame eval weaknesses.

[comment from Adam] I wonder if we need to think about extensions to counter this, where
we have mate threat, recapture, check extensions. This selective pushes back on pruning.
